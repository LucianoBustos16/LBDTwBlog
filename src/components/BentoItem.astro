---
const {title, subtitle } = Astro.props
---

<article
    class={`
        ${Astro.props.class}
        relative rounded-xl
        backdrop-blur-md
        border border-white/10
        col-span-5
    `}>
    <h2>{title}</h2>
    {subtitle && <h3>{subtitle}</h3>}
</article>